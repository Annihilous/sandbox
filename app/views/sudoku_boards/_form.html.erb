<%= form_with(model: sudoku_board) do |form| %>
  <% if sudoku_board.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sudoku_board.errors.count, "error") %> prohibited this sudoku_board from being saved:</h2>

      <ul>
        <% sudoku_board.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :grid_size, style: "display: block" %>
    <%= form.number_field :grid_size %>
  </div>

  <div>
    <%= form.label :grid_content, style: "display: block" %>
    <%= form.text_area :grid_content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
